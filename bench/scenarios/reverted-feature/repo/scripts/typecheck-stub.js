// This fixture repo has no real TypeScript build; this stands in for `tsc --noEmit`
// so verify_cmd in the scenario's artifact docs is a real, runnable command.
console.log("typecheck ok (stub)");
process.exit(0);
